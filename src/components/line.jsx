import react from "react";

const Line = ({margin}) =>{
    return(
        <>
        <div className={`${margin} border-b border-gray-700`} >
        
        </div></>
    );
};

export default Line;